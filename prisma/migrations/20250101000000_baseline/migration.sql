-- Baseline: this database already contained application tables before Prisma Migrate history existed.
-- No DDL here — only records a starting point in `_prisma_migrations` so later migrations can run.
SELECT 1;
